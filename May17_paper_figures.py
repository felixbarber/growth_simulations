#!/usr/bin/env python

import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt

# This script loads existing subfigures and combines them to make a single figure

def plotter(paths, array):
    for i0 in range(len(paths)):
        temp =
    return fig